# 🚀 White Broker Launcher - Руководство

## Описание

White Broker Launcher - это Ionic React приложение, которое служит центральной точкой для запуска и управления всеми приложениями White Broker из репозитория.

## 🎯 Возможности

### Главная страница (Launcher)
- 📱 Просмотр всех доступных приложений в репозитории
- ✅ Статус каждого приложения (запущено/остановлено)
- 🔗 Два режима открытия:
  - **Встроенный режим** - открывает приложение внутри Ionic в iframe
  - **Новая вкладка** - открывает приложение в отдельной вкладке браузера
- 📊 Информация о поддерживаемых платформах
- 🎨 Красивый современный дизайн с карточками

### Поддерживаемые приложения

1. **Flutter App** 
   - Порт: 8080
   - Платформы: iOS, Android, Web, Desktop
   - Технология: Flutter + Dart

2. **.NET MAUI App**
   - Порт: 5000
   - Платформы: iOS, Android, Windows, macOS
   - Технология: .NET MAUI + C#

3. **Ionic React App** (текущее)
   - Порт: 5173
   - Платформы: iOS, Android, Web, PWA
   - Технология: Ionic + React + TypeScript

## 📋 Быстрый старт

### 1. Запуск Ionic Launcher

```bash
cd whitebroker_ionic/white-broker
npm install  # если еще не установлены зависимости
npm run dev
```

Launcher будет доступен на `http://localhost:5173`

### 2. Запуск других приложений

#### Flutter App
```bash
cd whitebroker_app
flutter run -d chrome --web-port=8080
```

#### .NET MAUI App (если поддерживает web)
```bash
cd whitebroker_maui
dotnet run
```

### 3. Использование Launcher

1. Откройте Launcher в браузере: `http://localhost:5173`
2. Вы увидите главную страницу с карточками всех приложений
3. Запустите нужное приложение (см. команды выше)
4. Обновите страницу Launcher
5. Нажмите на одну из кнопок:
   - **"Открыть встроенно"** - для просмотра внутри Ionic
   - **"Открыть в новой вкладке"** - для открытия в отдельном окне

## 🏗️ Структура проекта

```
whitebroker_ionic/white-broker/src/
├── pages/
│   ├── Home.tsx              # Главная страница Launcher
│   ├── Home.css              # Стили Launcher
│   ├── WebViewPage.tsx       # Страница с встроенным iframe
│   ├── WebViewPage.css       # Стили WebView
│   ├── Page.tsx              # Обычные страницы
│   └── Page.css
├── components/
│   ├── Menu.tsx              # Боковое меню
│   └── Menu.css
├── App.tsx                   # Главный компонент с роутингом
└── main.tsx                  # Точка входа
```

## 🛠️ Технические детали

### Роутинг

```typescript
/                    -> редирект на /home
/home                -> Launcher (главная страница)
/webview/:appName    -> WebView с iframe для приложения
/folder/:name        -> Обычные страницы (из starter template)
```

### Конфигурация портов

Порты настраиваются в файле `src/pages/Home.tsx`:

```typescript
const apps: AppInfo[] = [
  {
    name: 'Flutter App',
    devPort: 8080,  // Порт для Flutter
    urlPath: 'flutter',
    // ...
  },
  // ...
];
```

И в файле `src/pages/WebViewPage.tsx`:

```typescript
const appUrls: Record<string, string> = {
  flutter: 'http://localhost:8080',
  maui: 'http://localhost:5000',
  ionic: 'http://localhost:5173',
};
```

### Статусы приложений

Статус определяется вручную в коде. Для автоматического определения можно добавить проверку доступности порта:

```typescript
// Пример проверки статуса
const checkAppStatus = async (port: number) => {
  try {
    const response = await fetch(`http://localhost:${port}`);
    return response.ok ? 'running' : 'stopped';
  } catch {
    return 'stopped';
  }
};
```

## 🎨 Кастомизация

### Добавление нового приложения

1. Откройте `src/pages/Home.tsx`
2. Добавьте новое приложение в массив `apps`:

```typescript
{
  name: 'My New App',
  description: 'Описание приложения',
  framework: 'Framework Name',
  icon: 'https://example.com/icon.png',
  platforms: ['iOS', 'Android'],
  color: '#FF5722',
  devPort: 3000,
  status: 'stopped',
  urlPath: 'myapp',
}
```

3. Добавьте URL в `src/pages/WebViewPage.tsx`:

```typescript
const appUrls: Record<string, string> = {
  // ...
  myapp: 'http://localhost:3000',
};
```

### Изменение дизайна

Все стили находятся в файлах:
- `src/pages/Home.css` - стили главной страницы
- `src/theme/variables.css` - глобальные цвета и переменные

## 📱 Особенности работы с iframe

### Ограничения

1. **CORS** - некоторые приложения могут блокировать загрузку в iframe
2. **X-Frame-Options** - серверы могут запрещать embedding
3. **Performance** - iframe может быть медленнее отдельной вкладки

### Решения

Если приложение не загружается в iframe:
1. Проверьте настройки CORS в приложении
2. Используйте режим "Открыть в новой вкладке"
3. Настройте заголовки сервера:
   ```
   X-Frame-Options: SAMEORIGIN
   Content-Security-Policy: frame-ancestors 'self'
   ```

## 🔧 Разработка

### Запуск в dev режиме
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Запуск тестов
```bash
npm run test.unit
npm run test.e2e
```

## 📄 API

### Компоненты

#### Home
Главная страница launcher с карточками приложений.

**Props**: нет

**Функции**:
- `handleOpenApp(app, embedded)` - открывает приложение
- `getPlatformIcon(platform)` - возвращает иконку платформы
- `getStatusColor(status)` - возвращает цвет для статуса
- `getStatusText(status)` - возвращает текст статуса

#### WebViewPage
Страница с встроенным iframe для отображения приложений.

**URL параметры**:
- `appName` - имя приложения (flutter/maui/ionic)

**Функции**:
- `handleIframeLoad()` - обработчик загрузки iframe
- `handleIframeError()` - обработчик ошибок iframe

## 🐛 Решение проблем

### Приложение не открывается

1. **Проверьте, что dev-сервер запущен**
   ```bash
   # Проверка портов Windows
   netstat -ano | findstr :8080
   ```

2. **Проверьте URL в консоли браузера**
   Откройте DevTools (F12) и посмотрите сетевые запросы

3. **Проверьте CORS настройки**
   Убедитесь, что приложение разрешает запросы с `localhost:5173`

### Кнопки неактивны

1. Проверьте статус в коде `Home.tsx`
2. Запустите нужное приложение
3. Обновите страницу launcher

### Iframe пустой

1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Console
3. Проверьте сетевые запросы в Network
4. Попробуйте режим "Открыть в новой вкладке"

## 📚 Дополнительно

- [Ionic Documentation](https://ionicframework.com/docs)
- [React Router Documentation](https://v5.reactrouter.com/)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)

---

**Создано**: 31 октября 2025  
**Версия**: 1.0.0  
**Автор**: AI Assistant










