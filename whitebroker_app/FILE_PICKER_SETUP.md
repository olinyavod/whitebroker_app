# Настройка выбора файлов в WebView

## ✅ Что было сделано

### 1. Разрешения в AndroidManifest.xml
Добавлены все необходимые разрешения для работы с файлами:
- `READ_EXTERNAL_STORAGE` (для Android ≤ 12)
- `WRITE_EXTERNAL_STORAGE` (для Android ≤ 12)
- `READ_MEDIA_IMAGES` (для Android 13+)
- `READ_MEDIA_VIDEO` (для Android 13+)
- `READ_MEDIA_AUDIO` (для Android 13+)
- `CAMERA` (для фото/видео с камеры)
- `MANAGE_EXTERNAL_STORAGE` (для полного доступа к файлам на Android 11+)

### 2. Intent queries
Добавлены queries для работы с файловыми интентами:
- `GET_CONTENT` - выбор файлов
- `PICK` - выбор из галереи
- `OPEN_DOCUMENT` - открытие документов
- `IMAGE_CAPTURE` - захват фото
- `VIDEO_CAPTURE` - захват видео

### 3. Флаг requestLegacyExternalStorage
Включен режим совместимости для Android 10 (API 29)

### 4. Плагин permission_handler
Добавлен плагин для запроса разрешений во время выполнения

### 5. Запрос разрешений в коде
При запуске приложения автоматически запрашиваются разрешения:
- Доступ к фото
- Доступ к видео
- Доступ к аудио
- Доступ к камере
- Доступ к хранилищу (для старых версий Android)

## 📱 Как проверить

1. Установите APK на устройство:
   ```bash
   flutter install
   # или
   adb install build/app/outputs/flutter-apk/app-debug.apk
   ```

2. При первом запуске приложение запросит разрешения - нажмите **"Разрешить"**

3. Откройте веб-страницу с полем выбора файлов (`<input type="file">`)

4. Нажмите на поле выбора файлов

5. Должен открыться системный диалог выбора файлов с опциями:
   - Документы
   - Галерея
   - Камера
   - Файловый менеджер

## 🔧 Настройки WebView

В коде включены следующие настройки:
- `JavaScriptMode.unrestricted` - поддержка JavaScript
- `enableDebugging(true)` - отладка WebView
- `setMediaPlaybackRequiresUserGesture(false)` - автовоспроизведение медиа
- `setGeolocationPermissionsPromptCallbacks` - разрешения геолокации

## ⚠️ Важные замечания

1. **Android 11+**: Для доступа ко ВСЕМ файлам (не только медиа) может потребоваться дополнительно запросить разрешение `MANAGE_EXTERNAL_STORAGE` через настройки системы.

2. **Android 13+**: Разрешения разделены по типам медиа (фото, видео, аудио). Приложение запрашивает все типы.

3. **Веб-сайт**: Убедитесь, что на веб-странице правильно настроены атрибуты `<input type="file">`:
   ```html
   <!-- Для всех файлов -->
   <input type="file">
   
   <!-- Только изображения -->
   <input type="file" accept="image/*">
   
   <!-- Изображения с камеры -->
   <input type="file" accept="image/*" capture="camera">
   
   <!-- Множественный выбор -->
   <input type="file" multiple>
   ```

## 🐛 Решение проблем

### Проблема: Диалог выбора файлов не открывается
**Решение**: 
- Проверьте, что разрешения предоставлены в настройках приложения
- Переустановите приложение для повторного запроса разрешений
- Проверьте логи: `adb logcat | grep -i webview`

### Проблема: Разрешения не запрашиваются
**Решение**:
- Полностью удалите приложение
- Очистите кеш: `flutter clean`
- Пересоберите: `flutter build apk --debug`
- Установите заново

### Проблема: Файлы не загружаются на сервер
**Решение**:
- Это проблема на стороне веб-сайта, а не приложения
- Проверьте CORS настройки на сервере
- Проверьте размер загружаемых файлов
- Проверьте консоль браузера в WebView (включена отладка)

## 📝 Файлы, которые были изменены

1. `android/app/src/main/AndroidManifest.xml` - добавлены разрешения и queries
2. `lib/main.dart` - добавлен запрос разрешений
3. `pubspec.yaml` - добавлен плагин permission_handler
4. `android/app/build.gradle.kts` - удалено исключение file_picker

## 🚀 Дополнительные возможности

Если нужна более продвинутая работа с файлами (например, предпросмотр, мультивыбор с UI), можно добавить плагин `file_picker` и создать собственный интерфейс выбора файлов.



