# 🐛 Отладка JavaScript на веб-сайте

## Три способа отладки

### 🔥 Способ 1: Встроенная консоль (САМЫЙ ПРОСТОЙ!)

В приложении теперь есть **встроенная JavaScript консоль**!

#### Как использовать:

1. **Запустите приложение** на устройстве
2. **Найдите синюю кнопку** 🐛 в правом нижнем углу экрана
3. **Нажмите на неё** - откроется консоль JavaScript снизу
4. **Взаимодействуйте с сайтом** - все логи будут отображаться в консоли

#### Что отслеживается:

- ✅ **console.log()** - обычные логи (синий цвет)
- ⚠️ **console.warn()** - предупреждения (оранжевый цвет)
- ❌ **console.error()** - ошибки (красный цвет)
- 💥 **Необработанные ошибки** - JavaScript runtime errors
- 🚫 **Отклоненные промисы** - unhandled promise rejections

#### Пример использования:

```
1. Откройте приложение
2. Нажмите на кнопку 🐛 (bug report)
3. Попробуйте выбрать файл на сайте
4. Смотрите в консоль - все ошибки будут там!
```

#### Очистка консоли:

- Нажмите кнопку **"Очистить"** (иконка clear_all) в заголовке консоли

---

### 💻 Способ 2: Chrome DevTools (ДЛЯ ПРОДВИНУТЫХ)

Более мощный инструмент для отладки через компьютер.

#### Инструкция:

1. **Подключите устройство** к компьютеру по USB
2. **Включите отладку по USB** на устройстве:
   - Настройки → Для разработчиков → Отладка по USB
3. **Откройте Chrome** на компьютере
4. **Введите в адресной строке**: `chrome://inspect`
5. **Найдите** ваше приложение "White Broker" в списке устройств
6. **Нажмите "inspect"** - откроется DevTools

#### Что можно делать:

- 🔍 Просматривать все элементы DOM
- 📊 Анализировать сетевые запросы
- 🐞 Ставить точки остановки (breakpoints)
- 📈 Профилировать производительность
- 💾 Просматривать LocalStorage, SessionStorage, Cookies
- 🎨 Изменять CSS в реальном времени

---

### 📱 Способ 3: ADB Logcat (ДЛЯ ЭКСПЕРТОВ)

Просмотр логов через командную строку.

#### Команды:

```bash
# Все логи WebView
adb logcat | grep -i webview

# JavaScript Console логи
adb logcat | grep "JavaScript Console"

# Только ошибки
adb logcat | grep -E "ERROR|Exception"

# Очистить и начать заново
adb logcat -c && adb logcat | grep -i webview
```

---

## 🔍 Примеры ошибок и их значение

### Ошибка выбора файла:

```
ERROR: Cannot read property 'files' of null
```
**Проблема**: На сайте пытаются обратиться к элементу `<input type="file">`, которого нет.

```
ERROR: SecurityError: Failed to read the 'files' property from 'FileList'
```
**Проблема**: Проблема с разрешениями. Проверьте, что все разрешения предоставлены.

```
WARN: No file selected
```
**Проблема**: Пользователь закрыл диалог выбора файла без выбора.

### Ошибки загрузки файла:

```
ERROR: Network request failed
```
**Проблема**: Проблема с интернет-соединением или CORS на сервере.

```
ERROR: 413 Request Entity Too Large
```
**Проблема**: Файл слишком большой для загрузки на сервер.

```
ERROR: 401 Unauthorized
```
**Проблема**: Требуется авторизация для загрузки файла.

---

## 🎯 Как проверить проблему с выбором файлов

### Шаг 1: Откройте встроенную консоль

Нажмите кнопку 🐛 в приложении.

### Шаг 2: Попробуйте выбрать файл

Нажмите на кнопку выбора файла на сайте.

### Шаг 3: Проверьте консоль

Смотрите, появились ли ошибки:

#### ✅ Если видите:
```
LOG: File selected: image.jpg
LOG: File size: 1234567 bytes
```
**Всё хорошо!** Файл выбран успешно.

#### ❌ Если видите:
```
ERROR: Failed to open file picker
ERROR: Permission denied
```
**Проблема с разрешениями!** Проверьте настройки приложения.

#### ❌ Если видите:
```
ERROR: input.click() is not working
ERROR: FileReader is not supported
```
**Проблема на стороне сайта!** Сообщите разработчикам веб-сайта.

---

## 🔧 Дополнительные инструменты отладки

### Проверка элемента input на сайте

Добавьте в консоль Chrome (через chrome://inspect):

```javascript
// Найти все input элементы для файлов
document.querySelectorAll('input[type="file"]');

// Проверить атрибуты
const input = document.querySelector('input[type="file"]');
console.log('Accept:', input.accept);
console.log('Multiple:', input.multiple);
console.log('Capture:', input.capture);

// Тест выбора файла
input.addEventListener('change', (e) => {
  console.log('Files selected:', e.target.files.length);
  for (let file of e.target.files) {
    console.log('File:', file.name, file.size, file.type);
  }
});
```

### Проверка поддержки API

```javascript
// Проверить доступные API
console.log('File API supported:', typeof File !== 'undefined');
console.log('FileList API supported:', typeof FileList !== 'undefined');
console.log('FileReader API supported:', typeof FileReader !== 'undefined');
console.log('FormData API supported:', typeof FormData !== 'undefined');
```

---

## 📞 Что делать, если ничего не помогает?

1. **Сделайте скриншот консоли** с ошибками
2. **Проверьте версию Android** (Настройки → О телефоне)
3. **Проверьте разрешения** (Настройки → Приложения → White Broker → Разрешения)
4. **Попробуйте на другом сайте** с выбором файлов (например, Gmail)
5. **Переустановите приложение** полностью

---

## ⚡ Быстрые проверки

### ✓ Чеклист проверки файлов:

- [ ] Разрешения предоставлены в настройках?
- [ ] Появляется ли диалог выбора файла?
- [ ] Есть ли ошибки в консоли?
- [ ] Работает ли выбор файлов в браузере Chrome на том же устройстве?
- [ ] Размер файла не превышает лимит сервера?
- [ ] Формат файла поддерживается сайтом?

---

## 🎓 Полезные ссылки

- [HTML File API](https://developer.mozilla.org/en-US/docs/Web/API/File_API)
- [WebView File Upload](https://developer.android.com/guide/webapps/webview#file-upload)
- [Chrome DevTools Guide](https://developer.chrome.com/docs/devtools/)

---

**Создано**: 30 октября 2025
**Версия приложения**: 1.0.0







