# Руководство по отладке JavaScript в приложении

## Что было исправлено

1. **Более ранняя инициализация логирования**: Теперь JavaScript-логгер устанавливается на событии `onPageStarted`, а не `onPageFinished`, что позволяет отслеживать все события, включая те, что происходят во время загрузки страницы.

2. **Защита от ошибок**: Все вызовы `ConsoleLogger.postMessage` обернуты в `try-catch`, чтобы избежать прерывания скриптов при ошибках.

3. **Двойная проверка**: В `onPageFinished` проверяется, был ли установлен логгер в `onPageStarted`. Если нет - устанавливается снова.

4. **Автоматическое сканирование страницы**: При загрузке страницы автоматически выполняется поиск всех `input[type="file"]` и кнопок на странице.

## Новые возможности в приложении

### 1. Встроенная консоль отладки
- **Синяя кнопка с иконкой жука** (внизу справа) - открывает/закрывает консоль JavaScript
- В консоли отображаются все логи, ошибки и предупреждения из JavaScript
- Кнопка **"Очистить"** в консоли - очищает все логи

### 2. Кнопка тестирования загрузки файлов
- **Зеленая кнопка с иконкой загрузки** (вверху справа внизу) - запускает тест загрузки файлов
- При нажатии:
  - Ищет все `input[type="file"]` на странице
  - Пытается кликнуть на первый найденный input
  - Если не найдено - выводит список всех кнопок, которые могут быть связаны с загрузкой

### 3. Автоматическое логирование
При загрузке каждой страницы в консоль выводится:
- ✅ Сообщение о том, что JavaScript работает
- 📋 Текущий URL
- 🔘 Количество найденных кнопок
- 📎 Количество и детали всех `input[type="file"]`

## Как использовать

1. **Запустите приложение**:
   ```bash
   flutter run
   ```

2. **Откройте встроенную консоль**:
   - Нажмите на синюю кнопку с иконкой жука внизу справа
   - Вы увидите панель с логами JavaScript

3. **Проверьте, что JavaScript загрузился**:
   - Сразу после загрузки страницы в консоли должно появиться:
     ```
     [JS] LOG: 🔍 JavaScript Console Logging активирован (onPageStarted)
     [JS] LOG: ✅ Страница полностью загружена, JavaScript работает
     [JS] LOG: 📋 URL: http://89.104.65.16/login
     [JS] LOG: 🔘 Найдено кнопок/элементов: X
     [JS] LOG: 📎 Найдено input[type="file"]: Y
     ```

4. **Попробуйте нажать на кнопку загрузки файлов на сайте**:
   - Все логи, ошибки и действия будут отображаться в консоли

5. **Используйте кнопку тестирования**:
   - Нажмите на зеленую кнопку с иконкой загрузки
   - Посмотрите в консоль - там должны появиться результаты теста

## Что проверить

### Если в консоли ничего не появляется:
1. Убедитесь, что приложение пересобрано после изменений:
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

2. Проверьте, что в Android включена отладка WebView:
   - В коде уже добавлено `AndroidWebViewController.enableDebugging(true)` (строка 185 main.dart)

### Если кнопка загрузки не работает:
1. Проверьте в консоли, сколько `input[type="file"]` найдено
2. Если найдено 0 - возможно, на сайте используется другой механизм загрузки (например, через JavaScript)
3. Нажмите зеленую кнопку тестирования, чтобы найти все элементы, связанные с загрузкой

### Дополнительная отладка через Chrome DevTools:

Можно подключиться к WebView через Chrome:
1. Подключите устройство по USB
2. Включите отладку USB на устройстве
3. Откройте в Chrome: `chrome://inspect/#devices`
4. Найдите ваше приложение в списке и нажмите "inspect"
5. Теперь у вас будет полный доступ к DevTools, как для обычного веб-сайта

## Типичные проблемы и решения

### JavaScript не загружается
- **Причина**: Возможно, сайт блокирует выполнение JavaScript в WebView
- **Решение**: Проверьте в Chrome DevTools, есть ли CSP (Content Security Policy) ограничения

### Input file не реагирует на клики
- **Причина**: Возможно, сайт использует обертку над input (например, div или button)
- **Решение**: Найдите в консоли, какие кнопки связаны с загрузкой, и попробуйте кликнуть на них через JavaScript

### Диалог выбора файлов не открывается
- **Причина**: WebView требует специальной настройки для обработки загрузки файлов
- **Решение**: Проверьте, что в коде есть обработчик `setOnShowFileChooser` (это будет добавлено отдельно, если требуется)

## Следующие шаги

Если после всех проверок загрузка файлов всё ещё не работает, нужно будет добавить обработчик выбора файлов в Android-специфичный код. Это требует настройки `AndroidWebChromeClient` с методом `onShowFileChooser`.







