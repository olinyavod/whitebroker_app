# Переменные для GitHub Repository - Краткая памятка

## ⚡ Минимальная конфигурация - НЕ ТРЕБУЕТСЯ НИЧЕГО!

Workflow будет работать **без дополнительных настроек**. Просто:
```bash
git commit -m "#Release v1.0 - Описание изменений"
git push origin main
```

## 📊 Все доступные переменные (все НЕОБЯЗАТЕЛЬНЫЕ)

### Secrets (для Telegram уведомлений)

| Переменная | Обязательно | Назначение |
|------------|-------------|------------|
| `TELEGRAM_BOT_TOKEN` | ❌ Нет | Токен бота для уведомлений о сборке |

### Variables (для Telegram уведомлений)

| Переменная | Обязательно | Назначение |
|------------|-------------|------------|
| `TELEGRAM_CHAT_ID` | ❌ Нет | ID чата для получения уведомлений |

## 🔐 Для публикации в Google Play Store (будущее)

Когда понадобится публиковать в Google Play, добавьте:

| Переменная | Назначение |
|------------|------------|
| `ANDROID_KEYSTORE_BASE64` | Base64 keystore для релизной подписи |
| `ANDROID_KEYSTORE_PASSWORD` | Пароль keystore |
| `ANDROID_KEY_ALIAS` | Алиас ключа (например: `whitebroker`) |
| `ANDROID_KEY_PASSWORD` | Пароль ключа |

**Но пока этого НЕ требуется!** Debug keystore работает для тестирования и распространения.

## ✅ Что настроить в репозитории

### Settings → Actions → General → Workflow permissions
- ✅ **Read and write permissions** (для создания релизов)

Всё! 🎉

## 📝 Как добавить переменные (если понадобится)

1. **Secrets**: Settings → Secrets and variables → Actions → Secrets → New repository secret
2. **Variables**: Settings → Secrets and variables → Actions → Variables → New repository variable

---

📚 Полная документация: [.github/SETUP.md](SETUP.md)

