# Быстрый старт: Версионирование

## 📍 Где указывается версия

Файл: **`whitebroker_maui/WhiteBroker.csproj`**

```xml
<ApplicationDisplayVersion>1.0.0</ApplicationDisplayVersion>  <!-- Версия для пользователей -->
<ApplicationVersion>1</ApplicationVersion>                     <!-- Номер сборки -->
```

## 🚀 Способ 1: Автоматически (рекомендуется)

### Используйте скрипт `bump-version.ps1`:

```powershell
# Исправление бага (1.0.0 → 1.0.1)
./bump-version.ps1 -Type patch

# Новая функция (1.0.1 → 1.1.0)
./bump-version.ps1 -Type minor

# Крупное обновление (1.1.0 → 2.0.0)
./bump-version.ps1 -Type major
```

**С описанием изменений:**
```powershell
./bump-version.ps1 -Type minor -Message "Добавлена темная тема"
```

Скрипт автоматически:
- ✅ Увеличит обе версии
- ✅ Покажет изменения
- ✅ Предложит добавить в git и создать коммит

## 📝 Способ 2: Вручную

### Шаг 1: Откройте `whitebroker_maui/WhiteBroker.csproj`

### Шаг 2: Обновите версии

```xml
<!-- Было -->
<ApplicationDisplayVersion>1.0.0</ApplicationDisplayVersion>
<ApplicationVersion>1</ApplicationVersion>

<!-- Стало -->
<ApplicationDisplayVersion>1.1.0</ApplicationDisplayVersion>
<ApplicationVersion>2</ApplicationVersion>
```

### Шаг 3: Закоммитьте с префиксом `#Release`

```bash
git add whitebroker_maui/WhiteBroker.csproj
git commit -m "#Release v1.1.0 - Добавлена новая функция"
git push origin main
```

## ⚡ GitHub Actions запустится автоматически

После пуша в `main` с коммитом, начинающимся с `#Release`:

1. ✅ Соберет APK и AAB
2. ✅ Создаст Release `v1.1.0`
3. ✅ Загрузит файлы
4. ✅ Отправит уведомление в Telegram (если настроено)

## ⚠️ Важно

- **ВСЕГДА увеличивайте ApplicationVersion** (номер сборки)
- **Коммит ДОЛЖЕН начинаться с `#Release`** для запуска сборки
- **Каждая новая сборка** должна иметь уникальный номер

## 📚 Подробная документация

- [Полное руководство по версионированию](whitebroker_maui/docs/VERSIONING.md)
- [Настройка GitHub репозитория](.github/SETUP.md)
- [Описание workflow](.github/workflows/README.md)

