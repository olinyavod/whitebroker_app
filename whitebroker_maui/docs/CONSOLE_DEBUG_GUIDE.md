# Руководство по использованию консоли браузера

## Обзор

В приложение WhiteBroker MAUI добавлена встроенная консоль браузера для отладки JavaScript ошибок и логов.

⚠️ **ВАЖНО:** Консоль доступна **только в DEBUG режиме**. В Release сборках консоль автоматически отключается и скрывается для повышения производительности и безопасности.

## Возможности

### 1. **Перехват console.log**
Все сообщения, отправленные через `console.log()` в веб-странице, будут отображаться в консоли приложения.

```javascript
console.log('Это сообщение появится в консоли');
console.log({ user: 'John', age: 30 }); // Объекты автоматически форматируются
```

### 2. **Перехват ошибок**
Консоль автоматически перехватывает и отображает:
- `console.error()` - ошибки (красный цвет)
- `console.warn()` - предупреждения (оранжевый цвет)
- `console.info()` - информационные сообщения (голубой цвет)
- Глобальные JavaScript ошибки
- Необработанные Promise ошибки

### 3. **Дедупликация сообщений**
Если одно и то же сообщение повторяется несколько раз подряд, консоль показывает счётчик повторений:
```
[14:25:33] [LOG] (x5) Данные загружены успешно
```
Это предотвращает спам одинаковыми сообщениями и делает консоль более читаемой.

### 4. **Цветовая индикация**
- 🔴 **Красный** - ошибки (errors)
- 🟠 **Оранжевый** - предупреждения (warnings)
- 🔵 **Голубой** - информация (info)
- ⚪ **Белый** - обычные логи (log)

### 5. **Экспорт логов**
Возможность скопировать все логи в буфер обмена одной кнопкой для дальнейшего анализа или отправки в поддержку.

## Как использовать

### Открытие консоли
1. Запустите приложение
2. В правом нижнем углу экрана появится кнопка **"🔍 Console"**
3. Нажмите на кнопку, чтобы открыть панель консоли

### Закрытие консоли
- Нажмите кнопку **"✕"** в правом верхнем углу панели консоли

### Очистка консоли
- Нажмите кнопку **"Очистить"** в панели консоли, чтобы удалить все сообщения

### Копирование логов
- Нажмите кнопку **"📋 Копировать"**, чтобы скопировать все логи в буфер обмена
- Логи копируются в структурированном формате с временными метками
- Полезно для отправки отчётов об ошибках

## Примеры отладки

### Пример 1: Проверка загрузки данных
```javascript
fetch('/api/data')
  .then(response => {
    console.log('Данные получены:', response);
    return response.json();
  })
  .then(data => {
    console.log('JSON данные:', data);
  })
  .catch(error => {
    console.error('Ошибка загрузки данных:', error);
  });
```

### Пример 2: Отладка событий
```javascript
document.getElementById('myButton').addEventListener('click', () => {
  console.log('Кнопка нажата');
  try {
    // ваш код
  } catch (error) {
    console.error('Ошибка при обработке клика:', error);
  }
});
```

### Пример 3: Проверка переменных
```javascript
const userProfile = {
  name: 'Иван',
  email: 'ivan@example.com',
  role: 'admin'
};

console.log('Профиль пользователя:', userProfile);
console.info('Роль пользователя:', userProfile.role);
```

## Технические детали

### Как это работает

1. **JavaScript инъекция**: При загрузке страницы автоматически инжектируется JavaScript код, который перехватывает методы `console.*`

2. **Передача сообщений**: JavaScript отправляет сообщения в MAUI через специальный URL протокол `console://type/message`

3. **Обработка в C#**: MainPage.xaml.cs перехватывает эти URL и отображает сообщения в панели консоли

4. **Форматирование**: Объекты автоматически конвертируются в JSON для удобного чтения

### Структура сообщений

Каждое сообщение в консоли имеет формат:
```
[ЧЧ:ММ:СС] [ТИП] Сообщение
```

Например:
```
[14:25:33] [LOG] Console capture initialized
[14:25:34] [ERROR] Failed to load resource: net::ERR_CONNECTION_REFUSED
[14:25:35] [WARN] Deprecated API usage detected
```

## Отладка проблем

### Консоль не открывается
- Убедитесь, что приложение полностью загружено
- Проверьте, что кнопка "🔍 Console" видна на экране

### Сообщения не отображаются
1. Откройте консоль
2. Перезагрузите страницу (кнопка "Обновить" в случае ошибки)
3. Проверьте в консоли сообщение "Console capture initialized" - оно подтверждает, что перехват работает

### Сообщения обрезаются
- Очень длинные сообщения могут быть обрезаны
- Используйте `JSON.stringify()` для сложных объектов:
  ```javascript
  console.log(JSON.stringify(largeObject, null, 2));
  ```

## Ограничения

1. **Производительность**: Большое количество логов может замедлить приложение
2. **Размер сообщений**: Очень большие объекты могут не отображаться полностью
3. **Автоскролл**: Консоль автоматически прокручивается к последнему сообщению

## Советы по использованию

1. **Используйте в режиме разработки**: Консоль предназначена для отладки, не используйте её в production сборке
2. **Очищайте консоль**: Регулярно очищайте консоль для удобства чтения
3. **Используйте разные уровни логирования**: `console.log()` для обычных логов, `console.error()` для ошибок, `console.warn()` для предупреждений
4. **Добавляйте контекст**: Включайте в логи информацию о том, где происходит действие:
   ```javascript
   console.log('[UserService] Загрузка пользователя:', userId);
   ```

## Отключение консоли в production

Консоль автоматически отключается в Release конфигурации благодаря условной компиляции:

```csharp
#if DEBUG
    // Весь код консоли активен только в Debug режиме
    consoleToggleButton.IsVisible = true;
    AddConsoleMessage("info", "[DEBUG MODE] Console debugging enabled");
#else
    // В Release режиме консоль полностью скрыта
    consoleToggleButton.IsVisible = false;
    consolePanel.IsVisible = false;
#endif
```

### Преимущества такого подхода:

1. **Безопасность**: Логи не доступны конечным пользователям
2. **Производительность**: Нет накладных расходов на перехват console.log в production
3. **Размер приложения**: Весь код консоли исключается из Release сборки
4. **Автоматизация**: Не нужно помнить отключать консоль вручную перед релизом

### Проверка конфигурации:

**Debug режим (разработка):**
```bash
dotnet build -c Debug
```
- ✅ Кнопка "🔍 Console" видна
- ✅ Консоль работает
- ✅ Логи перехватываются

**Release режим (production):**
```bash
dotnet build -c Release
```
- ❌ Кнопка консоли скрыта
- ❌ Панель консоли недоступна
- ❌ Перехват логов отключен
- ✅ Максимальная производительность

## Поддержка

При обнаружении проблем с консолью:
1. Проверьте логи самой консоли на предмет ошибок инициализации
2. Убедитесь, что WebView корректно загружает страницу
3. Проверьте, что JavaScript на странице не блокируется Content Security Policy

---

**Версия**: 1.0  
**Дата**: 2025-11-05  
**Автор**: WhiteBroker Development Team

