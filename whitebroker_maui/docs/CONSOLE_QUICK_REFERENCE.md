# 🔍 Быстрая справка по консоли браузера

## Конфигурация

| Режим | Консоль | Кнопка | Перехват логов |
|-------|---------|--------|----------------|
| **Debug** | ✅ Включена | ✅ Видна | ✅ Работает |
| **Release** | ❌ Отключена | ❌ Скрыта | ❌ Выключен |

## Быстрый старт

### 1. Запуск в Debug режиме
```bash
# Visual Studio
Выберите Debug конфигурацию и нажмите F5

# Командная строка
dotnet build -c Debug
dotnet run
```

### 2. Открытие консоли
- Нажмите кнопку **"🔍 Console"** в правом нижнем углу
- Консоль откроется внизу экрана (300px высота)

### 3. Использование консоли

**Кнопки управления:**
- **📋 Копировать** - скопировать все логи в буфер обмена
- **Очистить** - удалить все сообщения
- **✕** - закрыть консоль

**Цвета сообщений:**
- 🔴 Красный = Ошибки (`console.error`)
- 🟠 Оранжевый = Предупреждения (`console.warn`)
- 🔵 Голубой = Информация (`console.info`)
- ⚪ Белый = Обычные логи (`console.log`)

## Примеры использования

### JavaScript в браузере:
```javascript
// Обычный лог
console.log('Страница загружена');

// Ошибка
console.error('Не удалось загрузить данные');

// Предупреждение
console.warn('Устаревший API');

// Объект
console.log('User:', { name: 'Ivan', age: 30 });
```

### Результат в консоли:
```
[14:25:33] [INFO] [DEBUG MODE] Console debugging enabled
[14:25:34] [LOG] Console capture initialized
[14:25:35] [LOG] Страница загружена
[14:25:36] [ERROR] Не удалось загрузить данные
[14:25:37] [WARN] Устаревший API
[14:25:38] [LOG] User: {"name":"Ivan","age":30}
```

## Дедупликация

Повторяющиеся сообщения автоматически группируются:

**Было:**
```
[14:25:33] [LOG] Tick
[14:25:34] [LOG] Tick
[14:25:35] [LOG] Tick
[14:25:36] [LOG] Tick
[14:25:37] [LOG] Tick
```

**Стало:**
```
[14:25:33] [LOG] (x5) Tick
```

## Копирование логов

Формат скопированных логов:
```
=== WhiteBroker Console Logs ===
Дата: 2025-11-05 14:25:33
Всего сообщений: 15
========================================

[14:25:30] [LOG] Console capture initialized
[14:25:31] [ERROR] (x3) Failed to load resource
[14:25:32] [WARN] Deprecated API usage
[14:25:33] [LOG] Data loaded successfully
...
```

## Условная компиляция

Весь код консоли обёрнут в `#if DEBUG`:

```csharp
#if DEBUG
    // Код консоли (только в Debug)
    private List<ConsoleMessage> consoleMessages;
    private void AddConsoleMessage(...) { ... }
    private async Task InjectConsoleCapture() { ... }
#endif
```

**Преимущества:**
- 🔒 Безопасность - логи не доступны пользователям
- ⚡ Производительность - нет накладных расходов в Release
- 📦 Размер - код консоли исключается из Release сборки
- 🤖 Автоматизация - не нужно отключать вручную

## Сборка для production

```bash
# Release сборка (без консоли)
dotnet build -c Release

# Создание APK (Android)
dotnet publish -c Release -f net8.0-android

# Создание IPA (iOS)
dotnet publish -c Release -f net8.0-ios
```

## Устранение проблем

### Консоль не открывается
✅ Проверьте, что используется Debug конфигурация  
✅ Убедитесь, что кнопка "🔍 Console" видна  
✅ Перезапустите приложение

### Логи не появляются
✅ Откройте консоль (нажмите "🔍 Console")  
✅ Проверьте сообщение "Console capture initialized"  
✅ Убедитесь, что страница полностью загружена

### Много одинаковых сообщений
✅ Уже исправлено! Используется дедупликация  
✅ Счётчик показывает количество повторений

## Технические детали

### Архитектура
1. **JavaScript Injection** - скрипт перехвата инжектируется при загрузке страницы
2. **URL Protocol** - сообщения передаются через `console://type/message`
3. **Event Handler** - `OnWebViewNavigating` перехватывает консольные URL
4. **UI Update** - сообщения добавляются в `VerticalStackLayout`

### Производительность
- Минимальные накладные расходы
- Дедупликация уменьшает количество UI элементов
- Автоскролл работает асинхронно
- Весь код удаляется в Release

## Полезные команды

```bash
# Проверка конфигурации
dotnet build --configuration Debug -v n | grep Configuration

# Очистка и пересборка
dotnet clean && dotnet build -c Debug

# Запуск в Debug с детальным выводом
dotnet run -c Debug --verbosity detailed
```

## Дополнительно

📄 Полная документация: `CONSOLE_DEBUG_GUIDE.md`  
🏗️ Структура проекта: `PROJECT_STRUCTURE.md`  
🚀 Быстрый старт: `QUICKSTART.md`

---

**Версия**: 1.1  
**Дата**: 2025-11-05  
**Статус**: ✅ Готово к использованию в Debug режиме

