# 🚫 Работа с бинарными файлами и артефактами сборки

## Проблема

APK файлы и другие бинарные артефакты сборки **НЕ должны** храниться в Git репозитории.

## ❌ Почему это плохо?

### 1. **Раздувание репозитория**
- APK файл весит ~24 MB
- Каждый коммит с APK добавляет этот размер в историю
- История Git становится огромной и медленной
- Клонирование репозитория занимает много времени

### 2. **Бессмысленность**
- APK можно собрать из исходного кода в любой момент
- Старые версии APK не нужны для разработки
- Бинарные файлы невозможно смёржить при конфликтах

### 3. **Проблемы с GitHub/GitLab**
- Лимиты на размер файлов (обычно 100 MB)
- Медленная работа с репозиторием
- Проблемы с CI/CD

## ✅ Что было исправлено

### 1. Обновлён `.gitignore`

Добавлены правила для игнорирования:

```gitignore
# Android related
*.apk          # APK файлы Android
*.aab          # Android App Bundle
*.aar          # Android Archive

# .NET MAUI / Xamarin related  
**/bin/        # Папки сборки
**/obj/        # Объектные файлы
**/.vs/        # Visual Studio папка

# APK and AAB files (build artifacts)
*.ipa          # iOS приложения
*.msix         # Windows MSIX пакеты
*.appx         # Windows AppX пакеты
```

### 2. Удалён APK из репозитория

```bash
git rm --cached WhiteBroker-Release.apk
```

Файл удалён из Git, но остался на диске.

## 📦 Правильная работа с артефактами сборки

### Локальная разработка

APK файлы можно хранить локально:
```
D:\easyprog\whitebroker_app\
├── WhiteBroker-Release.apk     ← Локально (игнорируется Git)
├── releases/                    ← Локальная папка для APK
│   ├── v1.0.0.apk
│   ├── v1.0.1.apk
│   └── v1.1.0.apk
└── .gitignore                   ← Игнорирует *.apk
```

### GitHub Releases

Для распространения APK используйте **GitHub Releases**:

1. **Создайте тег**:
```bash
git tag v1.0.0
git push origin v1.0.0
```

2. **Создайте Release на GitHub**:
   - Перейдите в раздел "Releases"
   - Нажмите "Create a new release"
   - Выберите тег v1.0.0
   - **Прикрепите APK файл** как asset
   - Опубликуйте

3. **Результат**:
   - APK доступен для скачивания
   - Не хранится в истории Git
   - Чёткая версионность

### CI/CD (GitHub Actions)

Автоматическая сборка и публикация:

```yaml
# .github/workflows/build.yml
name: Build APK

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        
      - name: Build APK
        run: |
          cd whitebroker_maui
          dotnet build -c Release -f net9.0-android
          
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          files: whitebroker_maui/bin/Release/net9.0-android/*.apk
```

## 📋 Чек-лист перед коммитом

Перед каждым коммитом проверяйте:

```bash
# Проверить статус
git status

# Если видите *.apk, *.aab, *.ipa - НЕ КОММИТЬТЕ!
# Убедитесь, что они в .gitignore
```

### ✅ Правильные файлы для коммита:
- `.cs`, `.xaml` - исходный код
- `.csproj` - файлы проекта
- `.md` - документация
- `.gitignore` - конфигурация Git
- `.yml`, `.json` - конфигурации

### ❌ НЕ коммитить:
- `*.apk`, `*.aab` - бинарники Android
- `*.ipa` - бинарники iOS
- `bin/`, `obj/` - результаты сборки
- `.vs/` - локальные настройки IDE
- Большие файлы (> 1 MB)

## 🔧 Исправление существующей истории

Если APK уже в истории Git, можно очистить:

### Вариант 1: BFG Repo-Cleaner (рекомендуется)
```bash
# Установить BFG
# https://rtyley.github.io/bfg-repo-cleaner/

# Очистить все APK из истории
bfg --delete-files '*.apk' --delete-files '*.aab'

# Очистить Git
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

### Вариант 2: git filter-branch (сложнее)
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch *.apk *.aab" \
  --prune-empty --tag-name-filter cat -- --all
```

⚠️ **Внимание:** После очистки истории нужно сделать force push:
```bash
git push origin --force --all
```

## 📝 Инструкция для команды

### Для новых разработчиков:

1. **Клонировать репозиторий**:
```bash
git clone https://github.com/youruser/whitebroker_app.git
```

2. **Собрать APK локально**:
```bash
cd whitebroker_maui
dotnet build -c Release -f net9.0-android
```

3. **APK будет здесь**:
```
bin/Release/net9.0-android/ru.easyprrog.whitebroker-Signed.apk
```

4. **НЕ коммитить APK**:
```bash
# APK автоматически игнорируется .gitignore
git status  # APK не должен появиться в списке
```

## 🎯 Резюме

### ✅ Правильно:
- Коммитить исходный код
- Собирать APK локально
- Публиковать APK через GitHub Releases
- Использовать CI/CD для автоматической сборки

### ❌ Неправильно:
- Коммитить `*.apk` файлы
- Коммитить `bin/`, `obj/` папки
- Хранить бинарники в Git истории
- Пушить большие файлы

---

**Важно:** Эти правила применяются ко всем бинарным артефактам:
- APK (Android)
- AAB (Android Bundle)
- IPA (iOS)
- MSIX/APPX (Windows)
- EXE (исполняемые файлы)

**Храните в Git только исходный код, из которого можно собрать приложение!**

---

**Версия:** 1.0  
**Дата:** 2025-11-05  
**Статус:** ✅ Применено к проекту WhiteBroker

