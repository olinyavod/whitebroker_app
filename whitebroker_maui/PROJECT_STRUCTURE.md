# Структура проекта .NET MAUI

## Общая структура

```
whitebroker_maui/
│
├── 📄 App.xaml                           # Определение приложения (стили, ресурсы)
├── 📄 App.xaml.cs                        # Логика инициализации приложения
├── 📄 MainPage.xaml                      # Главная страница с WebView (UI)
├── 📄 MainPage.xaml.cs                   # Логика главной страницы (code-behind)
├── 📄 MauiProgram.cs                     # Точка входа, конфигурация приложения
├── 📄 WhiteBroker.csproj                 # Файл проекта (зависимости, настройки)
├── 📄 WhiteBroker.sln                    # Solution файл для Visual Studio
├── 📄 README.md                          # Документация
├── 📄 QUICKSTART.md                      # Руководство быстрого старта
├── 📄 .gitignore                         # Игнорируемые файлы для Git
│
├── 📁 Platforms/                         # Платформо-специфичный код
│   ├── 📁 Android/
│   │   ├── AndroidManifest.xml          # Разрешения и настройки для Android
│   │   ├── MainActivity.cs              # Главная активность Android
│   │   └── MainApplication.cs           # Класс приложения Android
│   │
│   ├── 📁 iOS/
│   │   ├── Info.plist                   # Настройки и разрешения для iOS
│   │   ├── AppDelegate.cs               # Делегат приложения iOS
│   │   └── Program.cs                   # Точка входа для iOS
│   │
│   └── 📁 Windows/
│       ├── App.xaml                     # XAML для Windows
│       ├── App.xaml.cs                  # Код для Windows
│       └── Package.appxmanifest         # Манифест Windows приложения
│
└── 📁 Resources/                         # Ресурсы приложения
    ├── 📁 AppIcon/                       # Иконка приложения
    │   ├── appicon.svg                  # Основная иконка
    │   └── appiconfg.svg                # Передний план иконки
    │
    ├── 📁 Splash/                        # Экран-заставка
    │   └── splash.svg                   # Изображение splash screen
    │
    ├── 📁 Images/                        # Изображения
    │   └── dotnet_bot.svg               # Пример изображения
    │
    ├── 📁 Fonts/                         # Пользовательские шрифты
    │   └── (OpenSans шрифты добавляются при необходимости)
    │
    ├── 📁 Raw/                           # Сырые файлы (JSON, txt и т.д.)
    │
    └── 📁 Styles/                        # XAML стили
        ├── Colors.xaml                  # Цветовая палитра
        └── Styles.xaml                  # Стили элементов управления
```

## Ключевые файлы

### 🎯 MauiProgram.cs
Точка входа в приложение. Здесь настраивается:
- Регистрация сервисов
- Конфигурация шрифтов
- Логирование
- Другие настройки приложения

**Назначение:** Аналог `main()` в Flutter

### 🎨 App.xaml + App.xaml.cs
Класс приложения верхнего уровня:
- Глобальные ресурсы и стили
- Установка главной страницы
- Обработка жизненного цикла приложения

**Назначение:** Аналог `MyApp` в Flutter

### 📱 MainPage.xaml + MainPage.xaml.cs
Главная страница приложения:
- **XAML файл:** Декларативное описание UI (разметка)
- **C# файл:** Логика и обработка событий (code-behind)

**Назначение:** Аналог `WebViewPage` в Flutter

### ⚙️ WhiteBroker.csproj
Файл проекта MSBuild:
- Целевые платформы (Android, iOS, Windows, macOS)
- NuGet пакеты (зависимости)
- Настройки компиляции
- Ресурсы приложения

**Назначение:** Аналог `pubspec.yaml` в Flutter

## Платформо-специфичный код

### Android
```
Platforms/Android/
├── AndroidManifest.xml      → Разрешения (камера, интернет, файлы)
├── MainActivity.cs           → Точка входа для Android
└── MainApplication.cs        → Инициализация Android приложения
```

**Ключевые настройки:**
- `usesCleartextTraffic="true"` - разрешает HTTP
- Разрешения для камеры, интернета, файлов

### iOS
```
Platforms/iOS/
├── Info.plist               → Настройки и разрешения
├── AppDelegate.cs           → Делегат приложения
└── Program.cs               → Точка входа
```

**Ключевые настройки:**
- `NSAllowsArbitraryLoads` - разрешает HTTP
- `NSCameraUsageDescription` - описание доступа к камере
- `NSPhotoLibraryUsageDescription` - доступ к фото

### Windows
```
Platforms/Windows/
├── App.xaml                 → UI определение для Windows
├── App.xaml.cs              → Логика Windows приложения
└── Package.appxmanifest     → Манифест с возможностями
```

**Ключевые настройки:**
- Capabilities (интернет, камера, файлы)
- Размер окна по умолчанию

## Ресурсы

### AppIcon
- **appicon.svg** - векторная иконка приложения
- **appiconfg.svg** - передний план (foreground) иконки
- Автоматически генерируются в нужных размерах для всех платформ

### Splash Screen
- **splash.svg** - векторное изображение для заставки
- Автоматически масштабируется для разных экранов

### Styles
- **Colors.xaml** - палитра цветов приложения
- **Styles.xaml** - стили для элементов управления (Button, Label и т.д.)

## XAML vs Dart

### Flutter (Dart)
```dart
Center(
  child: Text(
    'Hello',
    style: TextStyle(fontSize: 20),
  ),
)
```

### MAUI (XAML)
```xml
<Label Text="Hello"
       FontSize="20"
       HorizontalOptions="Center" />
```

## Сравнение с Flutter структурой

| Flutter | .NET MAUI | Назначение |
|---------|-----------|------------|
| `lib/main.dart` | `MauiProgram.cs` + `App.xaml.cs` | Точка входа |
| `pubspec.yaml` | `WhiteBroker.csproj` | Зависимости |
| Dart виджеты | XAML + Code-behind | UI определение |
| `android/` | `Platforms/Android/` | Android код |
| `ios/` | `Platforms/iOS/` | iOS код |
| `assets/` | `Resources/` | Ресурсы |

## Полезные команды

### Работа с проектом
```bash
# Восстановление зависимостей
dotnet restore

# Очистка проекта
dotnet clean

# Сборка
dotnet build

# Запуск
dotnet run
```

### Управление зависимостями
```bash
# Добавить пакет
dotnet add package PackageName

# Обновить пакет
dotnet update package PackageName

# Список пакетов
dotnet list package
```

### Целевые платформы
```bash
# Android
dotnet build -f net8.0-android

# iOS
dotnet build -f net8.0-ios

# Windows
dotnet build -f net8.0-windows10.0.19041.0

# macOS
dotnet build -f net8.0-maccatalyst
```

## Расширения проекта

### Добавление новой страницы
1. Создайте `NewPage.xaml` и `NewPage.xaml.cs`
2. Определите UI в XAML файле
3. Добавьте логику в code-behind файл
4. Навигация: `await Navigation.PushAsync(new NewPage());`

### Добавление зависимости
1. Добавьте NuGet пакет в `.csproj` или через CLI
2. Зарегистрируйте сервисы в `MauiProgram.cs`
3. Используйте через DI в конструкторах

### Добавление платформенного кода
1. Создайте файл в соответствующей папке `Platforms/`
2. Используйте `#if` директивы для условной компиляции
3. Или используйте partial классы

## Дополнительная информация

- [Официальная документация MAUI](https://learn.microsoft.com/dotnet/maui/)
- [XAML синтаксис](https://learn.microsoft.com/dotnet/maui/xaml/)
- [Жизненный цикл приложения](https://learn.microsoft.com/dotnet/maui/fundamentals/app-lifecycle)
- [Навигация](https://learn.microsoft.com/dotnet/maui/fundamentals/navigation/)







