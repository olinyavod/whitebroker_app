# Быстрый старт - White Broker MAUI

## Предварительные требования

### Windows
1. **Visual Studio 2022** (версия 17.8 или новее) с установленными компонентами:
   - Рабочая нагрузка ".NET Multi-platform App UI development"
   - Android SDK
   - (Опционально) Windows App SDK для Windows приложений

2. **Или через командную строку:**
```bash
# Установите .NET 8.0 SDK
# Скачайте с: https://dotnet.microsoft.com/download/dotnet/8.0

# Установите MAUI workload
dotnet workload install maui
```

### macOS (для разработки iOS/macOS приложений)
1. **Visual Studio 2022 for Mac** или **Visual Studio Code**
2. **Xcode** 15+ (из App Store)
3. **.NET 8.0 SDK**
```bash
# Установите MAUI workload
dotnet workload install maui
```

## Шаг 1: Клонирование репозитория

```bash
git clone [URL_РЕПОЗИТОРИЯ]
cd whitebroker_app/whitebroker_maui
```

## Шаг 2: Восстановление зависимостей

```bash
dotnet restore
```

## Шаг 3: Запуск приложения

### На Android

#### Через Visual Studio:
1. Откройте `WhiteBroker.sln`
2. В панели инструментов выберите целевую платформу **Android**
3. Выберите эмулятор или подключенное устройство
4. Нажмите **F5** или кнопку **Выполнить**

#### Через командную строку:
```bash
# Запуск на эмуляторе/устройстве
dotnet build -f net8.0-android
dotnet run -f net8.0-android
```

### На iOS (только macOS)

#### Через Visual Studio for Mac:
1. Откройте `WhiteBroker.sln`
2. Выберите целевую платформу **iOS**
3. Выберите симулятор или устройство
4. Нажмите **Выполнить**

#### Через командную строку:
```bash
dotnet build -f net8.0-ios
dotnet run -f net8.0-ios
```

### На Windows

#### Через Visual Studio:
1. Откройте `WhiteBroker.sln`
2. Выберите целевую платформу **Windows**
3. Нажмите **F5**

#### Через командную строку:
```bash
dotnet build -f net8.0-windows10.0.19041.0
dotnet run -f net8.0-windows10.0.19041.0
```

### На macOS (только на Mac)

```bash
dotnet build -f net8.0-maccatalyst
dotnet run -f net8.0-maccatalyst
```

## Шаг 4: Сборка релизной версии

### Android APK

```bash
# Сборка APK
dotnet publish -f net8.0-android -c Release

# APK будет находиться в:
# bin/Release/net8.0-android/publish/
```

### iOS (требуется Apple Developer аккаунт)

```bash
dotnet publish -f net8.0-ios -c Release
```

### Windows

```bash
dotnet publish -f net8.0-windows10.0.19041.0 -c Release
```

## Решение проблем

### Ошибка: "The 'maui' workload is not installed"

Решение:
```bash
dotnet workload install maui
```

### Ошибка: "Android SDK not found"

Решение:
1. Установите Android SDK через Visual Studio Installer
2. Или установите Android Studio
3. Установите переменную окружения `ANDROID_HOME`

### Ошибка при сборке для iOS: "Xcode not found"

Решение:
1. Установите Xcode из App Store
2. Запустите Xcode и примите лицензионное соглашение
3. Выполните: `sudo xcode-select --switch /Applications/Xcode.app`

### Ошибка: "Unable to load page" в WebView

Проверьте:
1. **Android:** `usesCleartextTraffic="true"` в `AndroidManifest.xml`
2. **iOS:** `NSAllowsArbitraryLoads` в `Info.plist`
3. Интернет-соединение работает
4. URL доступен: http://89.104.65.16/login

### Приложение не запускается на эмуляторе Android

Решение:
1. Убедитесь, что эмулятор запущен
2. Проверьте список устройств: `adb devices`
3. Попробуйте очистить проект: `dotnet clean`

## Изменение URL

Отредактируйте файл `MainPage.xaml`:

```xml
<WebView x:Name="webView"
         Source="http://ваш-новый-url.com"
         ... />
```

## Дополнительные команды

### Очистка проекта
```bash
dotnet clean
```

### Список установленных workloads
```bash
dotnet workload list
```

### Обновление workloads
```bash
dotnet workload update
```

### Проверка установленных SDK
```bash
dotnet --list-sdks
```

## Следующие шаги

После успешного запуска приложения:

1. 📖 Изучите [полную документацию](README.md)
2. 🔍 Ознакомьтесь со [структурой проекта](README.md#структура-проекта)
3. 🎨 Настройте иконку и splash screen
4. 🚀 Подготовьте релизную сборку

## Полезные ссылки

- [Официальная документация .NET MAUI](https://learn.microsoft.com/dotnet/maui/)
- [WebView в MAUI](https://learn.microsoft.com/dotnet/maui/user-interface/controls/webview)
- [Публикация Android приложений](https://learn.microsoft.com/dotnet/maui/android/deployment/)
- [Публикация iOS приложений](https://learn.microsoft.com/dotnet/maui/ios/deployment/)

## Получение помощи

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Посмотрите [Issues](https://github.com/dotnet/maui/issues) на GitHub
3. Обратитесь к команде разработки

---

**Поздравляем!** Вы готовы к разработке с White Broker MAUI! 🎉






