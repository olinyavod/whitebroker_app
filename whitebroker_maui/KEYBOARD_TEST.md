# Быстрый тест исправления клавиатуры

## ✅ Что было исправлено
- ✅ Клавиатура больше не перекрывает поле ввода
- ✅ WebView автоматически прокручивается при появлении клавиатуры
- ✅ Вы можете видеть, что вводите
- ✅ Все платформы (Android, iOS, Windows) собираются без ошибок

## 🚀 Быстрая проверка

### Android
```bash
# Перейдите в папку проекта
cd whitebroker_maui

# Соберите для Android
dotnet build -f net9.0-android

# Запустите на устройстве/эмуляторе
dotnet run -f net9.0-android
```

### iOS (только на macOS)
```bash
# Перейдите в папку проекта
cd whitebroker_maui

# Соберите для iOS
dotnet build -f net9.0-ios

# Запустите на устройстве/симуляторе
dotnet run -f net9.0-ios
```

### Windows
```bash
# Перейдите в папку проекта
cd whitebroker_maui

# Соберите для Windows
dotnet build -f net9.0-windows10.0.19041.0

# Запустите приложение
dotnet run -f net9.0-windows10.0.19041.0
```

## 📋 Что тестировать

1. **Откройте приложение**
2. **Найдите любое поле ввода** на странице https://wb.easy-prog.ru
3. **Коснитесь поля ввода** - появится клавиатура
4. **Проверьте:**
   - ✅ Поле ввода видно над клавиатурой
   - ✅ Вы видите, что вводите
   - ✅ WebView прокрутился автоматически
   - ✅ Нет перекрытия контента

## 🧪 Дополнительные тесты

### Тест 1: Несколько полей ввода
1. Переключайтесь между разными полями ввода
2. WebView должен прокручиваться к каждому активному полю

### Тест 2: Длинная страница
1. Прокрутите страницу вниз
2. Найдите поле ввода внизу страницы
3. Коснитесь его - WebView должен прокрутиться так, чтобы поле было видно

### Тест 3: Закрытие клавиатуры
1. Откройте клавиатуру
2. **Android:** Нажмите кнопку "Назад" на устройстве
3. **iOS:** Прокрутите WebView вниз или коснитесь области вне поля ввода
4. Клавиатура должна закрыться, WebView вернётся в исходное положение

## 🔧 Если что-то не работает

### Проблема: Клавиатура всё ещё перекрывает поле
**Решение для Android:**
1. Откройте `Platforms/Android/MainActivity.cs`
2. Попробуйте изменить `SoftInput.AdjustResize` на `SoftInput.AdjustPan`
3. Пересоберите приложение

### Проблема: WebView не прокручивается
**Проверьте:**
1. Убедитесь, что кастомный WebViewHandler зарегистрирован в MauiProgram.cs
2. Проверьте логи на наличие ошибок
3. Попробуйте очистить проект: `dotnet clean`

### Проблема: Приложение не собирается
**Решение:**
```bash
# Очистите проект
dotnet clean

# Восстановите зависимости
dotnet restore

# Соберите снова
dotnet build -f net9.0-android
```

## 📝 Технические детали изменений

### Файлы изменены:
1. ✅ `Platforms/Android/MainActivity.cs` - добавлен WindowSoftInputMode
2. ✅ `Platforms/Android/WebViewHandler.cs` - создан новый кастомный обработчик
3. ✅ `Platforms/iOS/WebViewHandler.cs` - создан новый кастомный обработчик
4. ✅ `MauiProgram.cs` - зарегистрированы кастомные обработчики

### Ключевые изменения:

#### Android:
- **MainActivity.cs:**
  - `WindowSoftInputMode = SoftInput.AdjustResize` - изменяет размер окна при появлении клавиатуры
  - `Window?.SetSoftInputMode(SoftInput.AdjustResize)` - программная установка режима

- **WebViewHandler.cs:**
  - `ScrollBarStyle.InsideOverlay` - лучший скроллинг
  - `VerticalScrollBarEnabled = true` - включен вертикальный скроллинг
  - `DomStorageEnabled = true` - поддержка localStorage

#### iOS:
- **WebViewHandler.cs:**
  - `ContentInsetAdjustmentBehavior.Automatic` - автоматическая корректировка отступов
  - `KeyboardDismissMode.Interactive` - интерактивное скрытие клавиатуры
  - Поддержка современного API для JavaScript (iOS 14+)

## 📚 Дополнительная документация
Для подробной документации смотрите файл `KEYBOARD_FIX.md`

## ✅ Статус сборки
- ✅ Android: Сборка успешна (без ошибок и предупреждений)
- ✅ iOS: Сборка успешна (без ошибок и предупреждений)
- ✅ Windows: Сборка успешна (без ошибок и предупреждений)
- ⚠️ MacCatalyst: Требует дополнительной настройки (опционально)

